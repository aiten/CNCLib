<Window x:Class="CNCLib.GCode.GUI.Views.LoadOptionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:CNCLib.GCode.GUI.Views"
        xmlns:vm="clr-namespace:CNCLib.GCode.GUI.ViewModels"        
        mc:Ignorable="d"
        Title="Load options" Height="700" Width="500">
    <Window.DataContext>
        <vm:LoadOptionViewModel />
    </Window.DataContext>

    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" Margin="10,10,10,10">
            <Button Command="{Binding DialogOKCommand}"  Content="Ok" Width="100"/>
            <Button Command="{Binding DialogCancelCommand}"  Content="Cancel" Width="100"/>
        </StackPanel>

        <xctk:PropertyGrid DataContext="{Binding LoadOptionsValue}" SelectedObject="{Binding}">
        </xctk:PropertyGrid>
    </DockPanel>
</Window>
