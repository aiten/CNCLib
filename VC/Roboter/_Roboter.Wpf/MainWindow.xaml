<Window x:Class="Roboter.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Roboter.Wpf.ViewModel"
        Title="MainWindow" Height="350" Width="525" Closing="Window_Closing">
    <Window.DataContext>
        <vm:RoboterModel />
    </Window.DataContext>
    <StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="12"></ColumnDefinition>
                <ColumnDefinition Width="100"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox Name="myComPort" Text="{Binding ComPort}" Grid.Column="1" Grid.Row="1" />
            <Button Command="{Binding Path=CommandConnect}" Grid.Column="1" Grid.Row="2" Content="Connect"  />
            <Button Click="Button_ManualControlClick" Grid.Column="1" Grid.Row="3" Content="Manual Control" IsEnabled="{Binding Path=Connected}" />
        </Grid>
    </StackPanel>
</Window>
